{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run (GCC 15, C++20)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${fileDirname}",
      "preLaunchTask": "Build (GCC 15, C++20)",
      "MIMode": "lldb"
    }
  ]
}
